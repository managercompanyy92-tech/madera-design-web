// src/pages/catalog/index.js

export default function CatalogPage() {
  return (
    <main
      style={{
        minHeight: "100vh",
        background: "#000",
        color: "#fff",
        fontSize: "24px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      ТЕСТ /catalog
    </main>
  );
}
